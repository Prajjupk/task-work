
# TaskPilot — Alternate Project
A restructured Streamlit project that preserves core features (tasks, files, audit, analytics, communication)
while using a different code layout and UI.

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

Default test account: admin/admin
